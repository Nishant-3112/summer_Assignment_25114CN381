#include <stdio.h>

int main(){
    int n,ld ,product=1;
    printf("Enter Number =>");
    scanf("%d",&n);

    while(n!=0){
        ld=n%10;
        product*=ld;
        n=n/10;
    }
    printf("product of given digits is = %d",product);
    return 0;
}
